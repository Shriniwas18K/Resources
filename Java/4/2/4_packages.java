/*
 * Packages are grouping of related classes to promote
 * encapsulation into single unit.They are important as
 * 1) provide security using package level access
 * 2) modularity,maintainiblity
 * 3) name resolution identifying any class uniquely
 * 
 * packages have universla reverse domain name convention
 * 
 * one source can belong only to one package and it must
 * be declared only as the first line of the source,
 * empty source is valid.
 */
